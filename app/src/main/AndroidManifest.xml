<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.players.gif">

    <uses-permission android:name="android.permissin.INTERNET" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar"
        android:usesCleartextTraffic="true">
        <activity android:name="New_Login_Register_Request.NewMainActivity"></activity>
        <activity android:name=".PostActivity" />
        <activity android:name=".page.BoardActivity" />
        <activity android:name="New_Login_Register_Request.activity_newLogin" />
        <activity android:name="New_Login_Register_Request.activity_newRegister" />
        <activity android:name=".page.BoardActivity" />
        <activity android:name=".RegisterActivity" />
        <activity
            android:name=".MainActivity"
            android:label="NavigationViewTest" />
        <activity android:name=".TestActivity" />
        <activity android:name=".LoginActivity" />
        <activity
            android:name=".LoadingActivity"
            android:noHistory="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>